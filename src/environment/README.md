# Environments

I think it would be easiest to examine and edit the environments if they are saved as text files.

`//` comments out a line

` ` (Space) - Walkable empty space

`S` - Spawn point

`#` - Obstacle

`E` - Exit
